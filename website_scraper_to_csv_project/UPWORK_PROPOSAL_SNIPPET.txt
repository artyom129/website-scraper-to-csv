You can use this sentence in proposals:

I also have a portfolio project where I built a Python website scraper that extracts structured data from multiple pages, removes duplicates, and exports the results to a clean CSV file.